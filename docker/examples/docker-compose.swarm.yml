# =============================================================================
# Evolution Go (community fork) — Docker Swarm stack
#
# For a Traefik-fronted Swarm deployment. The volumes/network are declared
# external, so create them first:
#
#   docker network create --driver overlay network_public
#   docker volume create evolution_go_data
#
# The app talks to a Postgres you provide (set the POSTGRES_*_DB URLs below).
# Adjust the Traefik host to your domain, then:
#
#   docker stack deploy -c docker-compose.swarm.yml evolution_go
# =============================================================================


services:
  evolution_go:
    image: ghcr.io/felipeestevanatto/evo-gofork:${EVOGO_VERSION:-dev}
    networks:
      - network_public
    environment:
      SERVER_PORT: 4000
      CLIENT_NAME: evolution
      OS_NAME: Evo-GoFork

      # REQUIRED: change this.
      GLOBAL_API_KEY: change-me-to-a-long-random-key

      # Point these at your managed Postgres.
      POSTGRES_AUTH_DB: postgresql://evolution_user:evolution_pass@postgres:5432/evogo_auth?sslmode=disable
      POSTGRES_USERS_DB: postgresql://evolution_user:evolution_pass@postgres:5432/evogo_users?sslmode=disable
      DATABASE_SAVE_MESSAGES: "true"
      MESSAGE_RETENTION_DAYS: "365"

      CONNECT_ON_STARTUP: "true"
      WEBHOOK_FILES: "true"
      LOG_DIRECTORY: /app/data/logs
      DEBUG_ENABLED: "0"
      LOG_TYPE: console
      SWAGGER_ENABLED: "true"

      # Optional integrations.
      WEBHOOK_URL: ""
      AMQP_URL: ""
      AMQP_GLOBAL_ENABLED: "false"
      AMQP_GLOBAL_EVENTS: ""
      NATS_URL: ""
      NATS_GLOBAL_ENABLED: "false"
      MINIO_ENABLED: "false"
      MINIO_ENDPOINT: ""
      MINIO_ACCESS_KEY: ""
      MINIO_SECRET_KEY: ""
      MINIO_BUCKET: ""
      MINIO_USE_SSL: "false"
      PROXY_HOST: ""
      PROXY_PORT: ""
      PROXY_USERNAME: ""
      PROXY_PASSWORD: ""
      API_AUDIO_CONVERTER: ""
      API_AUDIO_CONVERTER_KEY: ""

    volumes:
      - evolution_go_data:/app/data

    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      labels:
        - traefik.enable=true
        - traefik.http.routers.evolution_go.rule=Host(`evolution.example.com`)
        - traefik.http.routers.evolution_go.entrypoints=websecure
        - traefik.http.routers.evolution_go.tls.certresolver=letsencryptresolver
        - traefik.http.routers.evolution_go.service=evolution_go
        - traefik.http.services.evolution_go.loadbalancer.server.port=4000
        - traefik.http.services.evolution_go.loadbalancer.passHostHeader=true

volumes:
  evolution_go_data:
    external: true
    name: evolution_go_data

networks:
  network_public:
    external: true
    name: network_public
